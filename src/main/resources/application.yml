spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/shortener
    username: postgres
    password: postgres

  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
    open-in-view: false

  flyway:
    enabled: true

server:
  port: 8080
